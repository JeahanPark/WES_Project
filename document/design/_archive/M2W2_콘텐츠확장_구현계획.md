# M2 Week 2 콘텐츠 확장 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** CSV 콘텐츠를 확장하고(아이템 15종, 레시피 8종, 몬스터 3종, 월드오브젝트 5종), 소비 아이템 사용 + 장비 스탯 적용 + 모닥불 체온 회복 시스템을 구현한다.

**Architecture:** CSV 데이터만 수정하면 콘텐츠가 자동 반영되는 기존 파이프라인을 활용한다. 소비/장비 시스템은 QuickSlotRegistry.UseSlot과 InventoryRegistry에 로직을 추가하고, 모닥불 효과는 WorldBuildingObject에 범위 판정을 추가한다.

**Tech Stack:** Unity 6, Netcode for GameObjects, CSV Info 시스템

---

## Task 1: CSV 콘텐츠 데이터 확장

**Files:**
- Modify: `Assets/CSVInfo/ItemInfo.csv`
- Modify: `Assets/CSVInfo/CraftInfo.csv`
- Modify: `Assets/CSVInfo/CraftMaterialInfo.csv`
- Modify: `Assets/CSVInfo/MonsterInfo.csv`
- Modify: `Assets/CSVInfo/DropTableItemInfo.csv`
- Modify: `Assets/CSVInfo/DropSourceInfo.csv`
- Modify: `Assets/CSVInfo/WorldObjectInfo.csv`
- Modify: `Assets/CSVInfo/WorldAreaInfo.csv`
- Modify: `Assets/CSVInfo/WorldAreaMonsterInfo.csv`
- Modify: `Assets/CSVInfo/BuildingInfo.csv`

- [ ] **Step 1: ItemInfo.csv 교체**

```csv
Id.INT,Name.STRING,Description.STRING,IsStackable.BOOL,MaxStack.INT,PrefabKey.STRING,IconKey.STRING,BuildingInfoId.INT
1,나무,나무를 가공하여 만든 재료,true,99,TestItem,wood_icon,0
2,돌,단단한 돌 재료,true,99,TestItem,stone_icon,0
3,검,날카로운 검,false,1,TestItem,sword_icon,0
4,모닥불,설치 가능한 모닥불,true,9,TestItem,campfire_icon,1
5,약초,숲에서 채집하는 치유 허브,true,99,TestItem,herb_icon,0
6,가죽,몬스터에서 얻는 질긴 가죽,true,99,TestItem,leather_icon,0
7,뼈,몬스터에서 얻는 단단한 뼈,true,99,TestItem,bone_icon,0
8,철광석,광석 바위에서 채굴,true,99,TestItem,ironore_icon,0
101,회복 포션,HP를 30 회복하는 포션,true,10,TestItem,potion_hp_icon,0
102,체온 포션,체온을 20 회복하는 포션,true,10,TestItem,potion_cold_icon,0
103,붕대,HP를 15 회복하는 붕대,true,20,TestItem,bandage_icon,0
201,나무 방패,방어력을 올려주는 방패,false,1,TestItem,shield_icon,0
202,철검,강력한 철제 검,false,1,TestItem,ironsword_icon,0
203,가죽 갑옷,가죽으로 만든 방어구,false,1,TestItem,leatherarmor_icon,0
301,횃불,주변을 밝히는 횃불,true,9,TestItem,torch_icon,2
```

- [ ] **Step 2: CraftInfo.csv 교체**

```csv
Id.INT,CategoryType.CRAFTCATEGORYTYPE,Name.STRING,Description.STRING,IconKey.STRING,Value01.INT,ResultCount.INT
1,Building,모닥불,체온을 올려주는 모닥불입니다,campfire_icon,4,1
2,Item,회복 포션,HP를 30 회복하는 포션입니다,potion_hp_icon,101,1
3,Item,체온 포션,체온을 20 회복하는 포션입니다,potion_cold_icon,102,1
4,Item,붕대,HP를 15 회복하는 붕대입니다,bandage_icon,103,2
5,Item,나무 방패,방어력을 올려주는 방패입니다,shield_icon,201,1
6,Item,철검,강력한 철제 검입니다,ironsword_icon,202,1
7,Item,가죽 갑옷,가죽으로 만든 방어구입니다,leatherarmor_icon,203,1
8,Building,횃불,주변을 밝히는 횃불입니다,torch_icon,301,1
```

- [ ] **Step 3: CraftMaterialInfo.csv 교체**

```csv
Id.INT,CraftId.INT,MaterialItemId.INT,RequiredCount.INT
1,1,1,5
2,1,2,2
3,2,5,3
4,3,5,2
5,3,1,2
6,4,5,1
7,4,1,1
8,5,1,8
9,5,6,2
10,6,8,5
11,6,1,3
12,6,7,2
13,7,6,5
14,7,7,3
15,8,1,3
16,8,6,1
```

- [ ] **Step 4: MonsterInfo.csv 교체**

```csv
Id.INT,Name.STRING,MaxHP.INT,PrefabKey.STRING,DropTableId.INT
1,Test01Monster,100,Test01Monster,1001
2,늑대,80,Test01Monster,1002
3,골렘,250,Test01Monster,1003
```

- [ ] **Step 5: DropTableItemInfo.csv 교체**

```csv
DropTableId.INT,RewardType.ENUM,RewardId.INT,Min.INT,Max.INT
1001,Item,1,1,3
1001,Item,2,1,1
1002,Item,6,1,2
1002,Item,7,0,1
1003,Item,2,3,5
1003,Item,8,1,3
1003,Item,7,1,2
2001,Item,1,2,5
2002,Item,2,1,3
2003,Item,5,1,3
2004,Item,8,1,2
2004,Item,2,1,3
```

- [ ] **Step 6: DropSourceInfo.csv 교체**

```csv
WorldObjectType.ENUM,SourceId.INT,DropTableId.INT
Monster,1,1001
Monster,2,1002
Monster,3,1003
WorldObject,1,2001
WorldObject,2,2001
WorldObject,3,2002
WorldObject,4,2003
WorldObject,5,2004
```

- [ ] **Step 7: WorldObjectInfo.csv 교체**

```csv
Id.INT,WorldObjectType.ENUM,Name.STRING,MaxHP.INT
1,WorldObject,참나무,50
2,WorldObject,소나무,80
3,WorldObject,바위,150
4,WorldObject,약초 덤불,30
5,WorldObject,철광석 바위,200
```

- [ ] **Step 8: WorldAreaInfo.csv 교체**

```csv
Id.INT,Name.STRING,MaxCount.INT,RespawnDelay.FLOAT
1,해안가,3,10.0
2,숲,4,15.0
3,산지,2,30.0
```

- [ ] **Step 9: WorldAreaMonsterInfo.csv 교체**

```csv
Id.INT,AreaId.INT,MonsterId.INT
1,1,1
2,2,2
3,3,3
```

- [ ] **Step 10: BuildingInfo.csv 교체**

```csv
Id.INT,Name.STRING,PrefabKey.STRING
1,모닥불,Campfire
2,횃불,Torch
```

- [ ] **Step 11: CraftConditionInfo.csv 유지 확인**

기존과 동일 (변경 없음):
```csv
Id.INT,CraftId.INT,ConditionType.CRAFTCONDITIONTYPE,ConditionValue.INT
1,1,MaxCold,50
```

- [ ] **Step 12: 임시 아이콘 생성**

기존 `wood_icon.png`를 복사하여 누락된 아이콘 파일 생성:
- `Assets/GameResource/Image/ItemIcon/herb_icon.png`
- `Assets/GameResource/Image/ItemIcon/leather_icon.png`
- `Assets/GameResource/Image/ItemIcon/bone_icon.png`
- `Assets/GameResource/Image/ItemIcon/ironore_icon.png`
- `Assets/GameResource/Image/ItemIcon/potion_hp_icon.png`
- `Assets/GameResource/Image/ItemIcon/potion_cold_icon.png`
- `Assets/GameResource/Image/ItemIcon/bandage_icon.png`
- `Assets/GameResource/Image/ItemIcon/shield_icon.png`
- `Assets/GameResource/Image/ItemIcon/ironsword_icon.png`
- `Assets/GameResource/Image/ItemIcon/leatherarmor_icon.png`
- `Assets/GameResource/Image/ItemIcon/torch_icon.png`

각 파일의 .meta에서 `textureType: 8`, `spriteMode: 1`로 설정.
Addressable Default Local Group에 등록.

- [ ] **Step 13: u_editor_asset refresh + 컴파일 확인**

- [ ] **Step 14: 커밋**

```bash
git add Assets/CSVInfo/ Assets/GameResource/Image/ItemIcon/ Assets/AddressableAssetsData/
git commit -m "CSV 콘텐츠 확장: 아이템 15종, 레시피 8종, 몬스터 3종, 월드오브젝트 5종"
```

---

## Task 2: 소비 아이템 사용 시스템

**Files:**
- Modify: `Assets/Scripts/InGameObjectData/QuickSlotRegistry.cs`
- Modify: `Assets/Scripts/Info/Generator/ConvertInfo/ItemInfo.cs` (참조만 — 자동생성이라 수정 불가)

현재 `QuickSlotRegistry.UseSlot()`에서 소비 아이템 사용 로직이 비어있다. 아이템 ID 범위(101~199)로 소비 아이템을 판별하고, ID별 효과를 적용한다.

- [ ] **Step 1: QuickSlotRegistry.cs의 UseSlot 메서드 수정**

```csharp
    public void UseSlot(int _slotIndex, InventoryRegistry _inventory)
    {
        if (_slotIndex < 0 || _slotIndex >= SLOT_COUNT)
            return;

        int itemInfoId = m_SlotItemInfoIds[_slotIndex];
        if (itemInfoId == 0)
            return;

        ItemData itemData = _inventory.GetItem(itemInfoId);
        if (itemData == null)
            return;

        // 건물 아이템: 배치 모드 진입
        if (itemData.Info.IsBuilding)
        {
            InGameController.Instance?.BuildingPlacementWorker?.StartPlacement(itemData.Info.Id);
            return;
        }

        // 소비 아이템 (ID 101~199): 효과 적용 + 인벤토리 차감
        if (itemInfoId >= 101 && itemInfoId <= 199)
        {
            if (UseConsumable(itemInfoId, _inventory))
            {
                OnSlotChanged?.Invoke(_slotIndex);
            }
        }
    }

    private bool UseConsumable(int _itemInfoId, InventoryRegistry _inventory)
    {
        var controller = UnityEngine.Object.FindFirstObjectByType<InGameController>();
        if (controller == null)
            return false;

        var player = controller.PlayWorker?.LocalPlayer;
        if (player == null)
            return false;

        switch (_itemInfoId)
        {
            case 101: // 회복 포션: HP +30
                player.AddHP(30);
                break;
            case 102: // 체온 포션: Cold +20
                player.AddCold(20);
                break;
            case 103: // 붕대: HP +15
                player.AddHP(15);
                break;
            default:
                return false;
        }

        _inventory.RemoveItem(_itemInfoId, 1);
        return true;
    }
```

- [ ] **Step 2: using 추가**

QuickSlotRegistry.cs 상단에 `using UnityEngine;` 추가 (FindFirstObjectByType 사용).

- [ ] **Step 3: 컴파일 확인**

- [ ] **Step 4: 커밋**

```bash
git add Assets/Scripts/InGameObjectData/QuickSlotRegistry.cs
git commit -m "소비 아이템 사용 시스템 구현 (회복포션/체온포션/붕대)"
```

---

## Task 3: 장비 스탯 적용 시스템

**Files:**
- Modify: `Assets/Scripts/InGameObjectData/InventoryRegistry.cs`
- Modify: `Assets/Scripts/WorldBaseObject/Player/PlayerCharacter.cs`

장비 아이템(ID 201~299)이 인벤토리에 추가/제거될 때 플레이어 스탯에 보너스를 적용/해제한다.

- [ ] **Step 1: InventoryRegistry에 장비 변경 이벤트 추가**

InventoryRegistry.cs에 이벤트 추가:

```csharp
    // 기존 필드 아래에 추가
    public event System.Action OnInventoryChanged;
```

`AddItem`, `RemoveItem`, `SwapSlots`, `Clear` 메서드 끝에 이벤트 발화:

AddItem 메서드 — `m_Slots[emptyIndex] = new ItemData(info, _count);` 다음 줄과 `m_Slots[i].AddCount(_count); return;` 다음 줄에:
```csharp
        OnInventoryChanged?.Invoke();
```

RemoveItem 메서드 — `return true;` 직전에:
```csharp
        OnInventoryChanged?.Invoke();
```

Clear 메서드 — for 루프 이후에:
```csharp
        OnInventoryChanged?.Invoke();
```

- [ ] **Step 2: PlayerCharacter에 장비 스탯 계산 메서드 추가**

PlayerCharacter.cs에 추가:

```csharp
    public void RecalculateEquipmentStats()
    {
        int bonusATK = 0;
        int bonusDEF = 0;

        var controller = InGameController.Instance;
        if (controller == null) return;

        var inventory = controller.ObjectDataWorker?.GetInventoryRegistry();
        if (inventory == null) return;

        var slots = inventory.GetSlots();
        for (int i = 0; i < inventory.SlotCount; i++)
        {
            if (slots[i] == null) continue;

            int itemId = slots[i].Info.Id;
            switch (itemId)
            {
                case 3:   // 검: ATK +5
                    bonusATK = System.Math.Max(bonusATK, 5);
                    break;
                case 201: // 나무 방패: DEF +5
                    bonusDEF = System.Math.Max(bonusDEF, 5);
                    break;
                case 202: // 철검: ATK +8
                    bonusATK = System.Math.Max(bonusATK, 8);
                    break;
                case 203: // 가죽 갑옷: DEF +3
                    bonusDEF = System.Math.Max(bonusDEF, 3);
                    break;
            }
        }

        SetATK(DEFAULT_ATK + bonusATK);
        SetDEF(DEFAULT_DEF + bonusDEF);
    }
```

- [ ] **Step 3: 인벤토리 변경 시 스탯 재계산 연결**

PlayerCharacter의 Start 또는 초기화 시점에서 인벤토리 이벤트 구독. 기존 초기화 로직 근처에 추가:

```csharp
    // 로컬 플레이어일 때 인벤토리 변경 구독
    private void SubscribeInventoryEvents()
    {
        var inventory = InGameController.Instance?.ObjectDataWorker?.GetInventoryRegistry();
        if (inventory != null)
        {
            inventory.OnInventoryChanged += RecalculateEquipmentStats;
        }
    }

    private void UnsubscribeInventoryEvents()
    {
        var inventory = InGameController.Instance?.ObjectDataWorker?.GetInventoryRegistry();
        if (inventory != null)
        {
            inventory.OnInventoryChanged -= RecalculateEquipmentStats;
        }
    }
```

로컬 플레이어 설정 시 `SubscribeInventoryEvents()` 호출, OnDestroy에서 `UnsubscribeInventoryEvents()` 호출.

- [ ] **Step 4: 컴파일 확인**

- [ ] **Step 5: 커밋**

```bash
git add Assets/Scripts/InGameObjectData/InventoryRegistry.cs Assets/Scripts/WorldBaseObject/Player/PlayerCharacter.cs
git commit -m "장비 스탯 적용 시스템 구현 (인벤토리 보유 시 ATK/DEF 보너스)"
```

---

## Task 4: 모닥불 체온 회복 효과

**Files:**
- Modify: `Assets/Scripts/WorldBaseObject/WorldBuildingObject.cs`

배치된 모닥불이 주변 5m 내 플레이어의 Cold(체온)를 초당 2씩 회복시킨다.

- [ ] **Step 1: WorldBuildingObject.cs 전체 교체**

```csharp
using UnityEngine;
using Unity.Netcode;

public class WorldBuildingObject : WorldBaseObject
{
    private const float EFFECT_INTERVAL = 1f;
    private const float CAMPFIRE_RANGE = 5f;
    private const int CAMPFIRE_COLD_RECOVERY = 2;

    private int m_BuildingInfoId;
    private float m_EffectTimer;

    public void SetBuildingInfoId(int _buildingInfoId)
    {
        m_BuildingInfoId = _buildingInfoId;
    }

    private void Update()
    {
        if (!NetworkManager.Singleton.IsServer)
            return;

        if (m_BuildingInfoId != 1) // 모닥불만 효과 적용
            return;

        m_EffectTimer += Time.deltaTime;
        if (m_EffectTimer < EFFECT_INTERVAL)
            return;

        m_EffectTimer = 0f;
        ApplyCampfireEffect();
    }

    private void ApplyCampfireEffect()
    {
        var colliders = Physics.OverlapSphere(transform.position, CAMPFIRE_RANGE);
        foreach (var col in colliders)
        {
            var player = col.GetComponent<PlayerCharacter>();
            if (player != null)
            {
                player.AddCold(CAMPFIRE_COLD_RECOVERY);
            }
        }
    }
}
```

- [ ] **Step 2: 빌딩 스폰 시 BuildingInfoId 전달 확인**

`BuildingPlacementWorker`에서 빌딩 스폰 시 `SetBuildingInfoId`를 호출하는지 확인. 호출하지 않으면 `InGamePlayWorker.SpawnBuilding` 또는 해당 스폰 메서드에서 `SetBuildingInfoId` 호출을 추가.

- [ ] **Step 3: 컴파일 확인**

- [ ] **Step 4: 커밋**

```bash
git add Assets/Scripts/WorldBaseObject/WorldBuildingObject.cs
git commit -m "모닥불 체온 회복 효과 구현 (5m 내 Cold +2/초)"
```

---

## Task 5: QA 테스트

**Files:**
- Modify: `Assets/Scripts/Manager/TestManager.cs`

- [ ] **Step 1: TestManager에 콘텐츠 확장 테스트 시나리오 추가**

```csharp
    public void TestContentExpansion()
    {
        StartCoroutine(CoTestContentExpansion());
    }

    private IEnumerator CoTestContentExpansion()
    {
        GameDebug.Log("[TestManager] TestContentExpansion 시작");

        var controller = Object.FindFirstObjectByType<InGameController>();
        if (controller == null) { GameDebug.LogError("[TestManager] InGameController 없음"); yield break; }

        var inventory = controller.ObjectDataWorker.GetInventoryRegistry();
        var quickSlot = controller.ObjectDataWorker.GetQuickSlotRegistry();
        inventory.Clear();

        // 1. 신규 아이템 추가 확인
        inventory.AddItem(5, 10); // 약초
        inventory.AddItem(6, 5);  // 가죽
        inventory.AddItem(7, 5);  // 뼈
        inventory.AddItem(8, 5);  // 철광석
        GameDebug.Log($"[TestManager] 약초:{inventory.GetItem(5)?.Count}, 가죽:{inventory.GetItem(6)?.Count}, 뼈:{inventory.GetItem(7)?.Count}, 철광석:{inventory.GetItem(8)?.Count}");

        // 2. 소비 아이템 제작 + 사용 테스트
        inventory.AddItem(101, 1); // 회복 포션
        quickSlot.Register(0, 101);
        var player = controller.PlayWorker?.LocalPlayer;
        int hpBefore = player?.HP ?? 0;
        player?.AddHP(-50); // HP 50 감소
        yield return null;
        int hpAfterDamage = player?.HP ?? 0;
        GameDebug.Log($"[TestManager] HP: {hpBefore} → 데미지 후 {hpAfterDamage}");

        quickSlot.UseSlot(0, inventory);
        yield return null;
        int hpAfterPotion = player?.HP ?? 0;
        GameDebug.Log($"[TestManager] 포션 사용 후 HP: {hpAfterPotion} (회복 포션 +30)");

        // 3. 장비 스탯 확인
        inventory.AddItem(202, 1); // 철검
        inventory.AddItem(201, 1); // 나무 방패
        yield return null;
        player?.RecalculateEquipmentStats();
        GameDebug.Log($"[TestManager] 장비 장착 후 ATK:{player?.GetATK()}, DEF:{player?.GetDEF()}");

        inventory.RemoveItem(202, 1); // 철검 제거
        yield return null;
        player?.RecalculateEquipmentStats();
        GameDebug.Log($"[TestManager] 철검 제거 후 ATK:{player?.GetATK()}, DEF:{player?.GetDEF()}");

        // 4. 몬스터 정보 확인
        var monsterList = Managers.Info.MonsterInfoList;
        foreach (var m in monsterList)
        {
            GameDebug.Log($"[TestManager] 몬스터: {m.Name}, HP:{m.MaxHP}, DropTable:{m.DropTableId}");
        }

        GameDebug.Log("[TestManager] TestContentExpansion 완료");
    }
```

- [ ] **Step 2: 플레이모드 진입 → 테스트 실행 → 콘솔 확인**

```
u_play(action: "control", sub_action: "enter")
u_play(action: "invoke", target: "TestManager", componentType: "TestManager", methodName: "TestContentExpansion")
u_console(logType: "error")
u_console(logType: "log")
```

예상 결과:
- 에러 0개
- 약초:10, 가죽:5, 뼈:5, 철광석:5
- HP 감소 후 포션으로 +30 회복 확인
- 장비 ATK/DEF 보너스 적용/해제 확인
- 몬스터 3종 정보 출력

- [ ] **Step 3: 커밋**

```bash
git add Assets/Scripts/Manager/TestManager.cs
git commit -m "M2W2 콘텐츠 확장 QA 테스트 시나리오 추가"
```
