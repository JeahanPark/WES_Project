## UI Review 결과 — 2026-06-05 15:30

**대상**: Phase4 통합 패스 — Intro(LoginPopup)·Lobby(LobbyPopup/Room)·Ingame(HUD/팝업/코어텐션)·Clear(ResultPopup)
**모드**: both (기능 QA 모드A + UI 시각검수 모드B)

---

### 모드 A — 기능 QA (전부 통과)

| 검증 | 결과 |
|---|---|
| 컴파일 에러 | 0 (refresh 후 u_console error 0) |
| CoreTensionOverlay 7슬롯 와이어링 | PASS — Cold1/2/3·HpVignette·DeathOverlay·DayNightTint(Image)·AmbientFog(RawImage) 전부 연결, sprite/texture 실존, 초기 alpha(추위/HP/death=0, tint=0.18) 명세 일치, raycastTarget=0 |
| InGameHUDWorker.m_CoreTensionOverlay | PASS — fileID 961387419 연결 |
| 플레이모드 TestCoreTensionOverlay | PASS 17 / FAIL 0 — 추위 3단계 누적 페이드(Cold 35/65/95), Cold 0 복귀, HP≤30% 적색 비네팅 펄스(alpha 0.90), HP 회복 시 해제, 낮→밤 틴트 색전환(0.49,0.54,0.59→0.12,0.15,0.25, alpha 0.18 유지) |
| 플레이모드 TestResultPopupBackground | PASS 6 / FAIL 0 — Clear→BgSuccess on/BgDefeat off, GameOver→BgDefeat on/BgSuccess off |
| PlayerStatusHUD 게이지 | PASS — HP/Cold 프레임 9-slice(Sliced), fill Simple+anchorMax(옵션A), gauge_fill_hp/cold 실존 |
| 버튼 SpriteSwap 3-state | PASS — LoginPopup 3·LobbyPopup 3·LobbyRoomPopup 2 버튼 전부 transition=SpriteSwap, HL/PR/DS sprite 연결(InputField는 ColorTint=정상) |

TestManager 추가 시나리오: `TestCoreTensionOverlay()`, `TestResultPopupBackground()` (기존 public 메서드 SetCold/SetHP/TakeDamage/ForcePhase/ShowResult 조합 + reflection 슬롯 읽기만 사용, 테스트전용 로직 없음).

---

### 🚨 자동 플래그 (코드 검사)

1. [A7/LOW] btn_frame_idle 9-slice border 과함
   - border (110,110,110,110), 원본 600x600. 버튼 sizeDelta (200,60).
   - 상하 border 합 220 >> 높이 60 → 테두리 압축. 좌우 220 > 폭 200.
   - 깨짐은 경미(Unity 자동 스케일다운)하나 의도 두께 뭉개짐.
   - → designer-b 권고

### ⚠️ 시각 검수: 발견

1. [설계관찰/MED] death_overlay 암전(G-14) 화면 미노출
   - 시각: 사망 시 암전 페이드가 안 보임
   - 수치: 사망 직후 DeathOverlay alpha=0.01 (완전 암전 1.0 미도달)
   - 원인: 사망→TriggerGameOver→ResultPopup.OnEnable의 SetIngameUIVisible(false)가 HUDWorker(오버레이 부모) 즉시 SetActive(false) → CoFadeImage 코루틴(1.8s) 첫 프레임 중단. 코드 로직 자체는 정상 발화(DeathTriggered=true).
   - → client(코드)/디렉터(연출 의도) 판단 필요

2. ~~[B3/LOW] InventoryPopup 배경 비대칭~~ → **철회(오판정)**. 우측 큰 나무 패널은 비대칭 배경이 아니라 DetailPanel(아이템 상세 패널, panel_frame, 우측 280px stretch) 정상 요소. 좌=슬롯그리드 / 우=상세패널 2단 구성. 초기 캡처가 아이템 미선택+적은 셀 상태라 빈 상세패널만 크게 보여 오인.

3. [I-4/INFO] LoginPopup/LobbyPopup InputField 스킨 미적용 (우선순위 중, 누락 허용 범위)

4. [DetailPanel/LOW] 아이템 미선택 시 DetailPanel이 빈 나무 패널로 크게 비어 보임 → placeholder 안내 권고(선택). designer-b 통지.

### ✅ 추가 검증 (항목 7 — 슬롯셀 FrameBg, client 추가 의뢰)

- 인벤 CellTemplate / 크래프트 CraftScrollCell 모두 FrameBg 자식 = slot_frame, Sliced, color 흰색 a=1, sibling#0. stretch sizeDelta(8,8)로 80x80 셀 +8px 감쌈.
- 핵심: m_BackgroundImage가 **루트 셀 Image**(인벤 fileID 100091 sprite=NULL a=0 / 크래프트 루트 sprite=NULL)를 가리킴 — FrameBg가 아님. 코드(ApplyStyle/RefreshBackgroundColor/SetSelected)가 덮어쓰는 color는 루트 Image뿐 → FrameBg 프레임 안 묻힘. 빈칸/채움/선택 전환 시 FrameBg.color 유지 보장(코드 경로상 변경 불가).
- 런타임: 셀 20클론 각 FrameBg 정상, 좌측 셀에서 프레임 가시. → PASS

### ✅ 통과 (시각)

- 톤 일관성: bg_title/bg_lobby/bg_clear_success/bg_clear_defeat 4종 동일 손그림 잉크 톤 + 해변/등대 구도 변주. Don't Starve 다크 톤 일관.
- 정서 대비(C-1/C-2): 성공=불 켜진 마을·주황 온기 vs 전멸=흑백·서리·암전. 명세 의도 정확, 대비 탁월.
- logo_main(I-2): 나무판 판각 손글씨. logo rect 900x300 가독성 OK.
- btn_frame_idle(I-3): 나무판·밧줄·금속못 모티프.
- 코어텐션 추위 3단계 오버레이: 과하지 않음 — 캐릭터/지형/게이지 가독성 유지하며 화면이 명확히 차가워짐.
- 퀵슬롯 8칸 균등: 전부 80x80, anchoredPos 간격 정확히 85px.
- 게이지: HP/Cold 동일 400x30 Gauge 구조, fill stretch 마스킹.
- 9-slice border: panel_frame 120 / slot_frame 90 (큰 셀 적용, 정상).

### 📋 요약

- 모드A: 기능 전부 통과 (컴파일 0, 코어텐션 17/17, ResultPopup 6/6, 와이어링 일치)
- 모드B 자동 플래그: 1건 (LOW)
- 모드B 시각 검수: 3건 (MED 1·LOW 1·INFO 1) / 통과 8항목
- 차단(blocker) 없음. MED 1건(death_overlay)은 연출 의도 결정 필요.

스크린샷: screenshots/2026-06-05-1530/ (HUD_initial, ColdOverlay_stage3, InventoryPopup, CraftPopup, ResultPopup_Defeat)

---

## 최종 재검증 (death (b) 구현 후) — 2026-06-05 추가

director 결정 (b) 별 Canvas 분리 채택 후 client+designer-b 구현분 재검증.

### ✅ 정적 검증 통과
| 항목 | 결과 |
|---|---|
| DeathOverlay 위치 이동 | `Canvas/DeathOverlay` 루트 직속(HUD 밖)으로 이동 확인 → HUD 비활성에도 생존 |
| Worker m_DeathOverlay 슬롯 | fileID 1779759609 재연결 유지 |
| PlayDeathFade 시퀀스 | CoPlayDeathFade: 1.8s 암전(yield return 완료 대기) → _onComplete(ShowResultPopup) 호출. 중복 트리거 시 콜백만 보장 |
| InGameController 호출 | TriggerGameOverClientRpc → overlay.PlayDeathFade(() => ShowResultPopup(GameOver)). overlay null이면 즉시 표시 폴백 |
| Clear(승리) 경로 | TriggerClearClientRpc → ShowResultPopup(Clear) 즉시(암전 없음) 확인 |
| **렌더순서(정적 판정)** | DeathOverlay=Canvas(sortOrder0) 자식 #3. PopupManager가 팝업 오픈 시 PopupRoot.SetAsLastSibling()(PopupManager.cs:80) → ResultPopup이 Canvas 마지막 형제로 이동 → **DeathOverlay 암전 위로 그려짐**. BgDefeat가 어둠에서 떠오름 보장 |
| btn_frame border 110→40 | idle/hover/disabled 전부 40 확인. 상하 압축 대폭 완화(60 높이 대비 80, 이전 220 대비 개선) |
| 셰이더 _MainTex | NightDarknessOverlay.shader에 `_MainTex ("Sprite Texture", 2D) = "white"` 추가 확인. 플레이 진입 로그에서 해당 상시에러 미출현 |

### ⚠️ 런타임 검증 미완 (환경 이슈)
- 포트 7777 UDP 소켓 누수 — 직전 플레이 종료 후 Unity 에디터 프로세스(PID 8636)가 7777을 풀지 않아 호스트 시작 실패(StartLocalHost ok=False) → 플레이어 미스폰 → death (b) 1.8초 암전 **실제 화면 1.8초 지속** + BgDefeat 떠오름 **시각 캡처** 미완.
- 코드/구조/렌더순서는 정적으로 전부 통과. 시각 1.8초 지속 확인만 에디터 재시작 후 잔여.
- 조치: 에디터 재시작(또는 포트 해제) 후 TriggerGameOver 트리거 → 암전 1.8s 캡처 → ResultPopup BgDefeat 가시 캡처 1회.
